# Android APK artifacts

Do not use this folder as the source of truth for release distribution.

The current standalone APK is generated from `android-app` as:

```txt
android-app/KePedimos-release-standalone.apk
```

Older APKs may be kept under `archive/` only for traceability. Do not send archived builds to users when diagnosing production connectivity.
