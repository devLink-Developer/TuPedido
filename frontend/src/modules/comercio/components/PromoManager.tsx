import { IntegrationPendingCard } from "../../../shared/components";

export function PromoManager() {
  return (
    <IntegrationPendingCard
      title="Promociones pendientes de contrato"
      description="El módulo /m/promociones queda visible pero sin simulaciones. Hace falta un backend específico para promociones y descuentos."
    />
  );
}
